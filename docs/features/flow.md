# Flow — Wrapper de Geração por Slug (`/flows/:slug`)

- [ ] Carregar `FlowModel` por slug da tabela `flows`
- [ ] Montar `extraContext` concatenando `content` dos modules vinculados como blocos markdown
- [ ] Renderizar `GenerationPanel` com o contexto montado
